using UnityEngine;

public class HelloWarld : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("helloWorld");  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
